<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Register</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">

    <style>
        .container {
            width: 600px;
            padding: 30px;
            background-color: #1a1a1a;
            border-radius: 10px;
            border: 2px solid #007bff;
            box-shadow: 0px 0px 20px rgba(0, 123, 255, 0.5);
        }
        .form-group label {
            font-weight: bold;
            font-size: 1.1rem;
        }
        .form-control {
            border: 2px solid #007bff;
            border-radius: 5px;
        }
        .form-control:focus {
            box-shadow: 0px 0px 10px rgba(0, 123, 255, 0.7);
            border-color: #0056b3;
        }
        .btn-primary {
            width: 100%;
            font-size: 1.1rem;
            font-weight: bold;
            transition: 0.3s;
        }
        .btn-primary:hover {
            background-color: #0056b3;
            transform: scale(1.05);
        }
        .login-link {
            display: block;
            text-align: center;
            margin-top: 15px;
            color: #007bff;
            font-weight: bold;
            text-decoration: none;
            transition: 0.3s;
        }
        .login-link:hover {
            color: #0056b3;
            text-decoration: underline;
        }
    </style>
</head>
<body class="bg-black text-light">
    <div class="container my-5">
        <h1 class="text-center text-primary fw-bold">User Register</h1>
        <form action="/register" method="post" enctype="multipart/form-data">
            <div class="form-group mb-3">
                <label for="name">Name</label>
                <input name="name" type="text" class="form-control bg-black text-light" id="name" placeholder="Enter name">
            </div>
            <div class="form-group mb-3">
                <label for="email">Email address</label>
                <input name="email" type="email" class="form-control bg-black text-light" id="email" placeholder="Enter email">
            </div>
            <div class="form-group mb-3">
                <label for="password">Password</label>
                <input name="password" type="password" class="form-control bg-black text-light" id="password" placeholder="Password">
            </div>
            <div class="form-group mb-4">
                <label for="profile">Profile</label>
                <input name="file" type="file" class="form-control bg-black text-light" id="profile">
            </div>
            <button type="submit" class="btn btn-primary">Register</button>

            <!-- Login Link Below Submit Button -->
            <a href="/login" class="login-link">Already have an account? Login</a>
        </form>
    </div>
</body>
</html>
